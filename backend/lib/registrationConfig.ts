/**
 * Registration page config (countdown target, etc.).
 * Set COUNTDOWN_TARGET to festival start date; uses placeholder if not set.
 */

/** Countdown target — festival start. ISO string or Date. */
export const COUNTDOWN_TARGET = new Date('2026-03-15T09:00:00'); // placeholder; update for real event
